name: lithenet
description: Flutter desktop shell for LitheNet powered by singbox-ffi.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.3.0
  path_provider: ^2.1.6
  singbox_ffi: ^0.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
