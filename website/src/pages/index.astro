---
// Redirect root to /en/ (with base path support)
const base = import.meta.env.BASE_URL || '/';
const normalizedBase = base.endsWith('/') ? base : `${base}/`;
return Astro.redirect(`${normalizedBase}en/`);
---
